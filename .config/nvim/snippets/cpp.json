{
  "std::cout": {
    "prefix": "std::cout",
    "body": [
      "std::cout << ${1} << '\\n';"
    ]
  },
  "#if": {
    "prefix": "#if",
    "body": [
      "#ifdef USE_DD4HEP",
      "${1}",
      "#endif"
    ]
  }
}
